{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "docker-compose up -d",
    "build:css": "yarn workspace portfolio-app build:css"
  },
  "dependencies": {
    "dotenv-cli": "^4.0.0"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "@types/node": "^16.0.0"
  }
}
